import React from "react";
import { motion } from "framer-motion";
import { Building2, Home, Users, ClipboardCheck, Megaphone, Handshake, ShieldCheck, Phone, Mail, MapPin, ArrowRight, CheckCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";

// Tailwind-only, single-file, one-page marketing site
// Company: TenantMatch — We find quality tenants for rentals from small homes to large buildings
// Notes:
// - Replace placeholder copy, phone, email, and addresses.
// - The form is non-functional; wire to your backend or Formspree.
// - Designed for fast customization.

const features = [
  {
    icon: <Megaphone className="w-6 h-6" />, 
    title: "Pro Marketing",
    desc: "Pro photos, listings across top platforms, and targeted ads to fill vacancies fast."
  },
  {
    icon: <ClipboardCheck className="w-6 h-6" />, 
    title: "Tenant Screening",
    desc: "Credit, employment & landlord references, income verification, and compliance."
  },
  {
    icon: <Users className="w-6 h-6" />, 
    title: "Showings & Tours",
    desc: "Coordinated in‑person and virtual tours with pre‑qualified applicants."
  },
  {
    icon: <Handshake className="w-6 h-6" />, 
    title: "Lease & Move‑in",
    desc: "Offer negotiation, e‑signature leases, key handoff, and inspection checklist."
  },
  {
    icon: <ShieldCheck className="w-6 h-6" />, 
    title: "Compliance",
    desc: "Local ordinance expertise, fair‑housing alignment, and document retention."
  },
  {
    icon: <Building2 className="w-6 h-6" />, 
    title: "All Property Sizes",
    desc: "From single‑family homes to multi‑unit buildings and large portfolios."
  },
];

const stats = [
  { value: "<14 days", label: "Typical time‑to‑tenant" },
  { value: "98%", label: "On‑time lease execution" },
  { value: "3,000+", label: "Listings optimized" },
];

const plans = [
  {
    name: "Fill‑Only",
    price: "75% of one month’s rent",
    bullets: [
      "Pro listing & ads",
      "Showings & screening",
      "Lease drafting & signing",
      "Move‑in checklist"
    ]
  },
  {
    name: "Fill + Guarantee",
    price: "100% of one month’s rent",
    highlight: true,
    bullets: [
      "Everything in Fill‑Only",
      "90‑day replacement guarantee",
      "Premium photo package",
      "Priority showings"
    ]
  },
  {
    name: "Portfolio",
    price: "Custom",
    bullets: [
      "Multi‑unit/building campaigns",
      "Owner portal setup",
      "Bulk screening",
      "Dedicated manager"
    ]
  }
];

const steps = [
  { num: 1, title: "Quick Intake", desc: "Tell us about your unit(s), ideal tenant profile, and timing." },
  { num: 2, title: "Market & Screen", desc: "We launch listings, capture leads, and qualify applicants." },
  { num: 3, title: "Lease & Handover", desc: "We finalize the lease, complete inspections, and hand off keys." },
];

export default function TenantFinderSite() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-white to-slate-50 text-slate-800">
      {/* Top Bar */}
      <header className="sticky top-0 z-40 backdrop-blur bg-white/70 border-b">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center justify-between h-16">
            <div className="flex items-center gap-2">
              <Home className="w-6 h-6 text-rose-600" />
              <span className="font-semibold tracking-tight">TenantMatch</span>
            </div>
            <nav className="hidden md:flex items-center gap-6 text-sm">
              <a href="#services" className="hover:text-rose-600">Services</a>
              <a href="#process" className="hover:text-rose-600">Process</a>
              <a href="#pricing" className="hover:text-rose-600">Pricing</a>
              <a href="#contact" className="hover:text-rose-600">Contact</a>
            </nav>
            <div className="flex items-center gap-3">
              <Button asChild className="hidden sm:inline-flex">
                <a href="#contact">Get Tenants Faster</a>
              </Button>
            </div>
          </div>
        </div>
      </header>

      {/* Hero */}
      <section className="relative">
        <div className="max-w-6xl mx-auto px-4">
          <div className="grid md:grid-cols-2 gap-10 py-16 md:py-24 items-center">
            <motion.div initial={{opacity:0, y:10}} animate={{opacity:1, y:0}} transition={{duration:0.5}}>
              <h1 className="text-4xl md:text-5xl font-bold tracking-tight leading-tight">
                Quality tenants. <span className="text-rose-600">Less vacancy.</span> Zero hassle.
              </h1>
              <p className="mt-4 text-lg text-slate-600">
                We help real estate owners fill rentals—from cozy houses to large buildings—with vetted tenants you can trust.
              </p>
              <div className="mt-6 flex flex-col sm:flex-row gap-3">
                <Button asChild size="lg">
                  <a href="#contact" className="flex items-center">Request a Free Rent‑Ready Review <ArrowRight className="ml-2 h-5 w-5"/></a>
                </Button>
                <Button asChild variant="outline" size="lg">
                  <a href="#pricing">See Pricing</a>
                </Button>
              </div>
              <div className="mt-6 grid grid-cols-3 gap-4">
                {stats.map((s, i) => (
                  <Card key={i} className="shadow-sm">
                    <CardContent className="p-4 text-center">
                      <div className="text-xl font-semibold">{s.value}</div>
                      <div className="text-xs text-slate-500">{s.label}</div>
                    </CardContent>
                  </Card>
                ))}
              </div>
            </motion.div>

            <motion.div initial={{opacity:0, y:10}} animate={{opacity:1, y:0}} transition={{duration:0.6, delay:0.1}}>
              <Card className="shadow-xl border-0">
                <CardHeader>
                  <CardTitle className="flex items-center gap-2"><Building2 className="h-5 w-5 text-rose-600"/> Free Vacancy Audit</CardTitle>
                </CardHeader>
                <CardContent className="space-y-3">
                  <div className="text-sm text-slate-600">Get a quick score of your listing’s rent‑readiness in under a minute.</div>
                  <div className="grid grid-cols-2 gap-3">
                    <Input placeholder="Property Address" />
                    <Input placeholder="Unit / Suite" />
                    <Input placeholder="Monthly Rent (USD/CAD)" />
                    <Input placeholder="# Bedrooms" />
                  </div>
                  <Textarea placeholder="Notes (e.g., availability date, pet policy)"/>
                  <Button className="w-full">Get My Audit</Button>
                  <p className="text-xs text-slate-400">By submitting, you agree to be contacted about your inquiry.</p>
                </CardContent>
              </Card>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Services */}
      <section id="services" className="py-16 md:py-24 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-end justify-between mb-8">
            <h2 className="text-3xl md:text-4xl font-bold tracking-tight">What we do</h2>
            <p className="text-sm text-slate-500">End‑to‑end tenant placement for every property size.</p>
          </div>
          <div className="grid md:grid-cols-3 gap-6">
            {features.map((f, i) => (
              <Card key={i} className="hover:shadow-md transition-shadow">
                <CardHeader className="space-y-1">
                  <div className="w-10 h-10 rounded-xl bg-rose-50 text-rose-600 grid place-content-center">
                    {f.icon}
                  </div>
                  <CardTitle className="text-xl">{f.title}</CardTitle>
                </CardHeader>
                <CardContent className="text-sm text-slate-600">{f.desc}</CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Process */}
      <section id="process" className="py-16 md:py-24 bg-slate-50">
        <div className="max-w-6xl mx-auto px-4">
          <h2 className="text-3xl md:text-4xl font-bold tracking-tight mb-8">How it works</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {steps.map((s) => (
              <Card key={s.num}>
                <CardHeader>
                  <CardTitle className="flex items-center gap-2"><span className="w-8 h-8 rounded-full bg-rose-600 text-white grid place-content-center text-sm">{s.num}</span> {s.title}</CardTitle>
                </CardHeader>
                <CardContent className="text-sm text-slate-600">{s.desc}</CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Social Proof */}
      <section className="py-16 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="grid md:grid-cols-3 gap-6 items-stretch">
            <Card className="md:col-span-2">
              <CardHeader>
                <CardTitle>Landlords trust TenantMatch</CardTitle>
              </CardHeader>
              <CardContent className="grid md:grid-cols-2 gap-6">
                {["Found tenants in a week!", "Screening saved us from a bad lease.", "Great communication." , "Flawless process for our 24‑unit."].map((q, i) => (
                  <div key={i} className="p-4 rounded-xl border bg-slate-50">
                    <p className="text-sm">“{q}”</p>
                    <p className="mt-3 text-xs text-slate-500">— Owner #{i+1}</p>
                  </div>
                ))}
              </CardContent>
            </Card>
            <Card>
              <CardHeader>
                <CardTitle>Why owners choose us</CardTitle>
              </CardHeader>
              <CardContent className="space-y-2 text-sm">
                {[
                  "Faster lease‑ups",
                  "Lower vacancy loss",
                  "Better tenant fit",
                  "Transparent reporting",
                ].map((b, i) => (
                  <div className="flex items-center gap-2" key={i}>
                    <CheckCircle className="w-4 h-4 text-rose-600"/>
                    <span>{b}</span>
                  </div>
                ))}
              </CardContent>
            </Card>
          </div>
        </div>
      </section>

      {/* Pricing */}
      <section id="pricing" className="py-16 md:py-24 bg-slate-50">
        <div className="max-w-6xl mx-auto px-4">
          <h2 className="text-3xl md:text-4xl font-bold tracking-tight mb-8">Simple pricing</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {plans.map((p, i) => (
              <Card key={i} className={p.highlight ? "ring-2 ring-rose-600" : ""}>
                <CardHeader>
                  <CardTitle className="flex items-center justify-between">
                    <span>{p.name}</span>
                    {p.highlight && <span className="text-xs text-rose-600">Best value</span>}
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  <div className="text-2xl font-semibold mb-3">{p.price}</div>
                  <ul className="space-y-2 text-sm text-slate-600">
                    {p.bullets.map((b, j) => (
                      <li key={j} className="flex gap-2 items-start"><CheckCircle className="w-4 h-4 mt-0.5 text-rose-600"/> <span>{b}</span></li>
                    ))}
                  </ul>
                  <Button asChild className="mt-4 w-full"><a href="#contact">Get Started</a></Button>
                </CardContent>
              </Card>
            ))}
          </div>
          <p className="mt-4 text-xs text-slate-500">Pricing may vary by market and property type. Ask about volume rates for portfolios.</p>
        </div>
      </section>

      {/* CTA */}
      <section className="py-16 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="rounded-3xl p-8 md:p-12 bg-rose-600 text-white grid md:grid-cols-2 gap-8 items-center">
            <div>
              <h3 className="text-2xl md:text-3xl font-semibold">Ready to fill your vacancy?</h3>
              <p className="mt-2 text-white/90">Book a 15‑minute consult and get a tailored plan for your unit or building.</p>
            </div>
            <div className="flex md:justify-end">
              <Button asChild size="lg" variant="secondary">
                <a href="#contact" className="flex items-center">Book my consult <ArrowRight className="ml-2 h-5 w-5"/></a>
              </Button>
            </div>
          </div>
        </div>
      </section>

      {/* Contact */}
      <section id="contact" className="py-16 md:py-24 bg-slate-50">
        <div className="max-w-6xl mx-auto px-4">
          <div className="grid md:grid-cols-2 gap-8 items-start">
            <div>
              <h2 className="text-3xl md:text-4xl font-bold tracking-tight">Contact us</h2>
              <p className="mt-3 text-slate-600">Tell us about your property and we’ll reply within one business day.</p>
              <div className="mt-6 space-y-3 text-sm">
                <div className="flex items-center gap-2"><Phone className="w-4 h-4 text-rose-600"/> <span>(555) 123‑4567</span></div>
                <div className="flex items-center gap-2"><Mail className="w-4 h-4 text-rose-600"/> <span>hello@tenantmatch.co</span></div>
                <div className="flex items-center gap-2"><MapPin className="w-4 h-4 text-rose-600"/> <span>123 Main St, Suite 200, Your City</span></div>
              </div>
            </div>
            <Card className="shadow-lg">
              <CardHeader>
                <CardTitle>Send a message</CardTitle>
              </CardHeader>
              <CardContent className="space-y-3">
                <div className="grid md:grid-cols-2 gap-3">
                  <Input placeholder="Full Name" />
                  <Input type="email" placeholder="Email" />
                  <Input placeholder="Phone" />
                  <Input placeholder="Property Type (House / 12‑plex / etc.)" />
                </div>
                <Textarea placeholder="How can we help?" />
                <Button className="w-full">Submit Inquiry</Button>
                <p className="text-xs text-slate-400">We respect your privacy and never share your info.</p>
              </CardContent>
            </Card>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-10 border-t bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex flex-col md:flex-row items-center justify-between gap-4">
            <div className="flex items-center gap-2 text-sm text-slate-600">
              <Home className="w-4 h-4 text-rose-600"/>
              <span>© {new Date().getFullYear()} TenantMatch</span>
              <span className="hidden md:inline">•</span>
              <a className="underline-offset-2 hover:underline" href="#">Privacy</a>
              <span>·</span>
              <a className="underline-offset-2 hover:underline" href="#">Terms</a>
            </div>
            <div className="flex items-center gap-2 text-sm">
              <span>Serving single homes to high‑rise buildings.</span>
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
}
